return {
  "akinsho/bufferline.nvim",
  dependencies = "nvim-tree/nvim-web-devicons",
  opts = {
    options = {
      mode = "tabs",
      -- Show only when more than 1
      always_show_bufferline = false,
    },
  },
}
